export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>OpenClaw LINE Bridge</h1>
      <p>Webhook endpoint: <code>/api/webhook</code></p>
      <p>Debug endpoint: <code>/api/debug</code></p>
    </main>
  );
}
